<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="polinema.ac.id.androiduistarter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.wkp.tiketbioskop.activities.Setting"></activity>
        <activity android:name="com.wkp.tiketbioskop.activities.WelcomeActivity" />
        <activity android:name="com.wkp.tiketbioskop.activities.Details" />
        <activity android:name="com.wkp.tiketbioskop.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.wkp.tiketbioskop.activities.MainActivity" />
    </application>

</manifest>